/* eslint-disable @typescript-eslint/no-unused-vars */
import { IUser } from '@/types'
import React from 'react'

const UsersAvatarDetails = ({ user }: { user: IUser }) => {
    // console.log(user);

    return (
        <div>UsersAvatarDetails</div>
    )
}

export default UsersAvatarDetails